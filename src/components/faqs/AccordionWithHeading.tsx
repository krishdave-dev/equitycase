'use client'
import React from 'react'
import {
    Accordion,
    AccordionContent,
    AccordionItem,
    AccordionTrigger,
} from "@/components/ui/accordion"
import { FAQQuestion } from '@/types/faqs'
const AccordionWithHeading = ({ heading, faqs }: { heading: string, faqs: FAQQuestion[] }) => {
    return (
        <>
         

        </>
    )
}

export default AccordionWithHeading
